{
  "name": "uplink",
  "version": "0.1.0",
  "description": "up!link MCP-native Cloudflare R2 upload service",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "wrangler deploy --dry-run",
    "make-key": "bun scripts/make-key.ts",
    "new-key": "bun scripts/new-key.ts",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.0",
    "@types/bun": "latest",
    "vitest": "^4.1.5",
    "wrangler": "^4.88.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.12.3",
    "fast-deep-equal": "^3.1.3",
    "fast-uri": "^3.1.2",
    "json-schema-traverse": "^1.0.0",
    "zod": "^4.4.3"
  }
}
