export const DEFAULT_TTL_SECONDS = 3600;
export const MAX_JSON_BYTES = 1024 * 1024;
export const MAX_FILENAME_LENGTH = 180;
export const JSON_CONTENT_TYPES = ["application/json", "application/mcp+json"];
export const SHORT_LINK_TTL_SECONDS = 7 * 24 * 60 * 60; // 7 days for short links created via upload --short
